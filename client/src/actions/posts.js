import React from 'react';
import axios form 'axios'; 
import {GET_POST, POST_ERRORS} from 'types';
import PropTypes from 'prop-types';
'prop-types';

const posts = () => {
    return (
        <div>
            
        </div>
    );
};

posts.propTypes = {};

export default posts;